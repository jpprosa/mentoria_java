***SEMANA1****

Definicao: o termo orientação a objetos significa organizar o mundo real como uma coleção de objetos
que incorporam estrutura de dados e um conjunto de operações que manipulam estes dados.

Abstração: Refere-se ao conceito de esconder a implementação complexa e mostrar apenas as funcionalidades essenciais ao usuário.
Por exemplo, quando você tem uma classe abstrata que não pode ser instanciada por si só, mas pode ser usada como uma base para outras classes.


***SEMANA2****

Exception: É um evento que ocorre durante a execução de um programa e que interrompe o fluxo normal de instruções.
Try-Catch: O bloco try contém código que pode lançar exceções. 
O bloco catch contém o código que será executado se uma exceção for lançada no bloco try.
Finally: O bloco finally será executado após a execução dos blocos try e catch, independentemente de uma exceção ter sido lançada ou não.
Throw: Você pode lançar exceções manualmente usando a palavra-chave throw.

Collection: É uma estrutura de dados que pode ser usada para armazenar e manipular um conjunto de elementos.

Imutabilidade: Refere-se à característica de um objeto que, uma vez criado, não pode ter seu estado alterado.
No exemplo:
1 - A classe Pessoa é declarada como final para que ninguém possa estendê-la evitando a quebra da imutabilidade por subclasses.
2 - Todos os campos da classe são privados e finais "private" garante que o campo não possa ser acessado ou modificado fora da classe, 
e "final" garante que o campo só possa ser inicializado uma vez e nunca reatribuído.
3 - Somente getters são fornecidos. Não há setters, o que garante que os campos não sejam modificados após a criação do objeto.
4 - O construtor inicializa os campos e, uma vez feito, esses campos não podem ser alterados.